# 子流程与调用活动规则

## 嵌入式子流程（SubProcess）

表示流程中嵌套的子流程。
BPMN 2.0 标准中，展开/折叠是视图行为，不作为独立类型。

**校验规则：**
- 必须有入线
- 必须有出线
- 可以有多条出线（并行分支）

## 调用活动（CallActivity）

表示调用外部流程定义。

**校验规则：**
- 必须有入线
- 必须有出线
- 出线数量为 1

---

> 返回 [[00-行为规则总览]]
